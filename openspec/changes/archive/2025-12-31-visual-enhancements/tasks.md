# Tasks
- [ ] Install `canvas-confetti` and `@types/canvas-confetti`.
- [ ] Update `Randomizer.vue` to trigger confetti.
- [ ] Update `MenuList.vue` and `HistoryList.vue` with transitions.
- [ ] Refine global and component CSS for "cool" look.
